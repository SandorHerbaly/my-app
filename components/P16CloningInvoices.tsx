import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const P15CloningInvoices: React.FC = () => {
  return (
    <div className="container mx-auto p-6">
      <Card>
        <CardHeader>
          <CardTitle>p15-Számlák klónozása generálta adatokból</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Ez az oldal jelenleg fejlesztés alatt áll. Itt lesz lehetőség a számlák klónozására a generált adatokból.</p>
        </CardContent>
      </Card>
    </div>
  );
};